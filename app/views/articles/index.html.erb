<table>
	<tr>
		<th>Title</th>
		<th>Article</th>
	</tr>

	<% @articles.each do |article| %>
	<tr>
		<td><%= article.title %></td>
		<td><%= article.text %></td>
	</tr>
	<% end %>

</table>

<%= link_to 'New Article', '/articles/new' %>